# Tasks: Create Project Structure

## 1. Directory Structure

- [x] 1.1 Create `components/` subdirectories: `ui`, `workout`, `exercise`, `charts`.
- [x] 1.2 Create `lib/` subdirectories: `db`, `validations`, `utils`, `constants`.
- [x] 1.3 Create `services/` directory.
- [x] 1.4 Create `types/` directory.

## 2. Placeholder Files

- [x] 2.1 Add `.gitkeep` to new directories to ensure they are tracked.
- [x] 2.2 Create `lib/utils.ts` (Shadcn utils placeholder).
- [x] 2.3 Create `services/index.ts`.

## 3. Verify

- [x] 3.1 Verify directory structure exists.
