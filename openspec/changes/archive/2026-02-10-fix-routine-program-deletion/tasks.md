## 1. Workout Card Deletion

- [x] 1.1 Implement `handleDelete` function in `components/workout/WorkoutCard.tsx`.
- [x] 1.2 Import and call `deleteWorkout` server action within `handleDelete`.
- [x] 1.3 Add `window.confirm` check before calling the server action.
- [x] 1.4 Implement `isDeleting` state to manage loading state on the delete button.
- [x] 1.5 Update the delete button to use the `handleDelete` function and show visual feedback when `isDeleting` is true.

## 2. Program Card Deletion

- [x] 2.1 Implement `handleDelete` function in `components/program/ProgramCard.tsx`.
- [x] 2.2 Import and call `deleteProgram` server action within `handleDelete`.
- [x] 2.3 Add `window.confirm` check before calling the server action.
- [x] 2.4 Implement `isDeleting` state to manage loading state on the delete button.
- [x] 2.5 Update the delete button to use the `handleDelete` function and show visual feedback when `isDeleting` is true.
